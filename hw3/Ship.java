public abstract class Ship extends Transport {
    public Ship(String model) {
        super(model);
    }

    // Тут могли бы быть какие-то методы предполагающиеся именно для кораблей
    // вроде определения скорости в узлах
}
